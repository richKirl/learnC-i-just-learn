Отличный конфиг. Вижу, что ты используешь libc++ и правильно готовишь std.pcm — это даст тебе самую быструю компиляцию и поддержку последних фишек. Раз std::execution (P2300) пока мимо, заменим его на продвинутую работу с потоками, атомиками и корутинами, которые в Clang 20 реализованы отлично.
Вот первая сотня задач. Идем строго от простого к сложному.
## Блок 1: Модули и C++26 Modern Syntax (1-25)
Тут вбиваем в пальцы новые правила написания кода.

   1. Static Hello: Создай модуль App, экспортируй функцию, возвращающую std::string_view с текстом.
   2. Import Std: Перепиши Hello World, используя только import std; (без #include).
   3. Placeholder Unused: Объяви std::lock_guard через плейсхолдер auto _ = ....
   4. Structured Bindings _: Распакуй std::tuple из 3 элементов, игнорируя средний через _.
   5. Pack Indexing First: Напиши вариативный шаблон, возвращающий args...[0].
   6. Pack Indexing Last: Напиши шаблон, возвращающий args...[sizeof...(args)-1].
   7. Static Assert Constexpr: Сделай static_assert, который вычисляет длину строки-сообщения в compile-time.
   8. Deducing This (Base): Реализуй структуру Point, где метод print() использует this self как явный параметр.
   9. Deducing This (Const): Напиши метод, который автоматически выбирает const или не-const версию через this Auto&& self.
   10. Module Partition Interface: Создай модуль Math, разделив его на Math:Add и Math:Sub через партиции.
   11. Private Module Fragment: Реализуй логику внутри module :private в одном .cppm файле.
   12. Nodiscard Logic: Экспортируй из модуля перечисление Error с атрибутом [[nodiscard]].
   13. Auto String Formatting: Используй std::print для вывода std::vector<int> напрямую.
   14. Floating Point Print: Выведи число pi с точностью 10 знаков через std::format_to.
   15. Saturating Add: Реализуй функцию, которая складывает uint8_t и не «перекручивается» выше 255.
   16. Saturating Sub: То же самое для вычитания (не ниже 0).
   17. Constexpr Lambda: Напиши лямбду, которая вычисляется в static_assert.
   18. Lambda Attributes: Примени атрибут [[deprecated]] к лямбда-выражению.
   19. Nested Namespaces: Объяви в модуле вложенное пространство имен App::UI::Themes коротким способом.
   20. Using Enum: Используй using enum внутри функции для сокращения кода.
   21. Explicit Bool: Напиши конструктор с explicit(sizeof(T) > 4).
   22. Consteval Constructor: Сделай тип, который можно создать только в compile-time.
   23. Module Global Fragment: Подключи <iostream> (старый стиль) в Global Module Fragment и смешай с import std;.
   24. Array Size Deduction: Используй std::array с автоматическим выводом размера в модуле.
   25. Variable Templates: Создай модульный шаблон переменной pi_v<T>.

## Блок 2: Ranges, Views и MDSPAN (26-50)
Фокус на работу с памятью и данными без лишних аллокаций.

   1. Range Sort: Отсортируй вектор через std::ranges::sort.
   2. Simple Filter: Создай views::filter, который оставляет только числа кратные 3.
   3. Transform View: Преобразуй вектор int в вектор double через views::transform.
   4. Enumerate View: Выведи элементы вектора с их индексами через views::enumerate.
   5. Zip View: Проитерируйся по двум массивам разной длины одновременно через views::zip.
   6. Take & Drop: Возьми первые 5 элементов, пропустив первые 2, используя пайплайн |.
   7. Views Join: Склей список строк в одну строку через views::join.
   8. MDSPAN 2D: Создай mdspan над массивом из 16 элементов как матрицу 4x4.
   9. MDSPAN Subspan: Вырежи из матрицы 4x4 центральный квадрат 2x2.
   10. MDSPAN Layout: Реализуй доступ к элементам матрицы в стиле Column-major (по столбцам).
   11. Ranges Find: Найди первый элемент в мапе, удовлетворяющий условию, через std::ranges::find_if.
   12. Ranges To Vector: Преобразуй результат цепочки views обратно в std::vector через std::ranges::to.
   13. Lazy Evaluation: Докажи ленивость views, добавив std::print внутрь фильтра.
   14. String View Split: Разбей строку по разделителю ; без создания новых строк.
   15. Reverse View: Разверни строку задом наперед без копирования.
   16. Custom Range: Создай свой класс, который можно использовать в for (auto x : my_range).
   17. Chunk View: Разбей вектор на куски (chunks) по 3 элемента.
   18. Slide View: Реализуй скользящее окно размером 2 по массиву.
   19. Adjacent View: Сравни текущий элемент со следующим в одном цикле.
   20. Repeat View: Создай бесконечный генератор одного числа и ограничь его через take.
   21. Cartesian Product: Получи все комбинации элементов из двух векторов.
   22. Stride View: Пройдись по контейнеру с шагом 3.
   23. Expected Return: Напиши функцию read_file, возвращающую std::expected<std::string, int>.
   24. Expected Monadic: Обработай результат expected через .and_then().
   25. Optional Transform: Используй .transform() для изменения значения внутри std::optional.

## Блок 3: Умная память и Метапрограммирование (51-75)
Задачи на владение и шаблоны.

   1. Unique Ptr Module: Передай владение объектом между двумя функциями в разных модулях.
   2. Shared Ptr Atomic: Используй std::atomic<std::shared_ptr<T>> для безопасной замены данных.
   3. Weak Ptr Observer: Реализуй кэш объектов через weak_ptr.
   4. Custom Deleter: Напиши unique_ptr для работы с FILE* из C.
   5. Small String Optimization: Экспериментально определи размер SSO для твоего Clang.
   6. Concept Basic: Напиши концепт Numeric, который принимает только int и float.
   7. Requires Clause: Сделай функцию, которая работает только если тип имеет метод serialize().
   8. Concept Composition: Объедини два концепта через &&.
   9. Constexpr If: Реализуй разную логику функции для целых и дробных чисел внутри одной функции.
   10. Type Traits: Используй std::is_constant_evaluated() для оптимизации алгоритма.
   11. Member Detector: Напиши шаблон, который проверяет наличие поля id в классе.
   12. Integer Sequence: Создай функцию, печатающую числа от 1 до N через шаблоны.
   13. Tuple For Each: Напиши функцию, которая применяет лямбду к каждому элементу std::tuple.
   14. Variant Visit: Создай std::variant<int, string>, и выведи его через std::visit.
   15. Any Cast: Сохрани произвольный объект в std::any и достань его.
   16. Source Location: Создай функцию log(), которая печатает имя файла и строку вызова автоматически.
   17. Stacktrace Basic: (Если доступно) Выведи текущий стек вызовов при ошибке.
   18. Alignment: Создай структуру с выравниванием alignas(64) для избежания False Sharing.
   19. Bit Cast: Преобразуй double в uint64_t побитово через std::bit_cast.
   20. Endian Swap: Смени порядок байт в числе через std::byteswap.
   21. Type Identity: Используй std::type_identity для подавления вывода типа в шаблоне.
   22. Non-type Template Parameter: Используй std::string (через структурный тип) как параметр шаблона.
   23. Move Only Function: Используй std::move_only_function (C++23) как колбэк.
   24. Function Ref: Передай тяжелую лямбду через std::function_ref.
   25. Compile-time Map: Реализуй поиск значения по ключу в constexpr массиве пар.

## Блок 4: Многопоточность и Атомики (76-100)
Синхронизация и параллелизм без execution.

   1. Jthread Start: Запусти std::jthread и убедись, что он сам делает join().
   2. Stop Token: Прерви цикл внутри потока через request_stop().
   3. Mutex Guard: Защити общий счетчик через std::mutex.
   4. Recursive Mutex: Реализуй рекурсивный вызов функции, захватывающей один и тот же мьютекс.
   5. Shared Mutex: Реализуй сценарий «много читателей, один писатель».
   6. Condition Variable: Напиши классическую очередь Producer-Consumer.
   7. Atomic Counter: Замени мьютекс на std::atomic<int> и сравни скорость.
   8. Atomic Flag: Реализуй простейший Spinlock.
   9. Atomic Wait: Заставь поток ждать изменения атомика через .wait().
   10. Memory Order Relaxed: Реализуй счетчик событий, где порядок не важен.
   11. Acquire/Release: Реализуй передачу флага готовности данных между потоками.
   12. Latch Sync: Подожди, пока 5 потоков инициализируются, прежде чем продолжать Main.
   13. Barrier Loop: Реализуй итеративный алгоритм, где 4 потока синхронизируются на каждом шаге.
   14. Semaphore Limit: Ограничь количество одновременных подключений к ресурсу до 3.
   15. Call Once: Инициализируй тяжелый ресурс внутри функции гарантированно один раз.
   16. Thread Local: Создай переменную, которая уникальна для каждого потока.
   17. Async Future: Запусти расчет в std::async и получи результат через 2 секунды.
   18. Packaged Task: Оберни функцию в задачу и передай её в другой поток.
   19. Atomic Exchange: Реализуй lock-free обновление указателя.
   20. Compare and Swap: Напиши безопасный инкремент через compare_exchange_weak.
   21. Hazard Pointers (Manual): Реализуй простейшую логику защиты узла списка от удаления другим потоком.
   22. Coroutine Task: Напиши простейшую корутину-генератор yield_next().
   23. Awaitable Sleep: Реализуй корутину, которая «спит» без блокировки потока.
   24. Thread Pool (Basic): Создай вектор jthread, который разбирает задачи из очереди.
   25. Final Boss: Напиши многопоточный сканер портов, использующий модули, std::print и jthread.

------------------------------
Как двигаться:
Бери первые 5 задач. Создавай файлы Task1.cppm, Task2.cppm и так далее. Линкуй их к main.cpp через свой скрипт.


Вторая сотня задач. Здесь мы уходим от базового синтаксиса в сторону архитектуры, системного программирования, метапрограммирования на шаблонах и продвинутой работы с памятью.
Поскольку у тебя Linux и Clang 20, фокус будет на системных вызовах через C++, глубоком использовании std::ranges, std::expected и создании надежных интерфейсов через модули.
## Блок 5: Глубокое метапрограммирование и шаблоны (101-125)
Фокус: вычисления в compile-time и генерация кода.

   1. Template String: Реализуй класс строки как параметр шаблона (NTTP), чтобы можно было писать MyTemplate<"hello">.
   2. Custom Concepts: Напиши концепт IsContainer, проверяющий наличие begin(), end() и value_type.
   3. Member Pointer: Напиши функцию, которая принимает указатель на поле класса и меняет его значение.
   4. Constexpr Map: Реализуй ассоциативный массив, который работает полностью в compile-time.
   5. Recursive Lambda: Напиши лямбду для вычисления факториала, используя this (C++23/26) для самовызова.
   6. Type List: Создай структуру, хранящую список типов, и напиши мета-функцию для поиска индекса типа в этом списке.
   7. Auto Return Deduction: Напиши сложный шаблон, где возвращаемый тип зависит от условий if constexpr.
   8. Pack Expansion in Bindings: Распакуй кортеж в аргументы функции через std::apply.
   9. Function Traits: Напиши шаблон, который извлекает тип возвращаемого значения и типы аргументов любой функции.
   10. SFINAE vs Concepts: Реализуй одну и ту же задачу (перегрузку для целых/плавающих) через enable_if и через requires, сравни читаемость.
   11. Injecting Names: Используй using namespace внутри модуля так, чтобы это не «протекло» наружу к пользователю.
   12. Constexpr Sort: Напиши std::ranges::sort, который работает в compile-time для std::array.
   13. Detection Idiom: Напиши проверку, можно ли объект T вывести в std::print.
   14. Small Vector: Реализуй свой SmallVector (стековый буфер + куча), работающий в constexpr.
   15. Overload Pattern: Напиши overloaded { ... } для std::visit через наследование от лямбд.
   16. Perfect Forwarding Module: Экспортируй из модуля функцию-обертку, которая идеально пробрасывает аргументы.
   17. Ref-qualified Methods: Напиши методы класса, которые ведут себя по-разному для & и && объектов.
   18. Custom Type Traits: Реализуй свой is_copy_constructible без использования стандартного.
   19. Compile-time Prime Checker: Статическая проверка числа на простоту до 1000.
   20. Lambda in NTTP: Передай лямбду как параметр шаблона класса.
   21. Constexpr Unique Ptr: Проверь, как работает std::unique_ptr внутри константного выражения.
   22. Static Reflection Sim: Используй макросы + шаблоны, чтобы имитировать рефлексию (получение имен полей).
   23. Fold Expressions: Реализуй функцию all_true(args...) через свертки.
   24. Curiously Recurring Template Pattern (CRTP): Реализуй статическое полиморфное поведение через модули.
   25. Mixins: Напиши систему классов-примесей через шаблоны.

## Блок 6: Системное программирование и Linux (126-150)
Фокус: взаимодействие с ОС, дескрипторы и файлы.

   1. File Descriptor Wrapper: Создай RAII-класс для int fd (open/close), экспортируй его как модуль.
   2. Memory Mapped File: Напиши обертку над mmap для чтения файла через указатель.
   3. Std::print to File: Используй std::print для записи в FILE* напрямую через дескриптор.
   4. Endianness Conversion: Напиши модуль для конвертации данных между Little-endian и Big-endian (сетевой порядок).
   5. Signal Handler: Установи обработчик SIGINT (Ctrl+C), который безопасно выставляет std::atomic<bool>.
   6. Unix Domain Sockets: Реализуй передачу строки между двумя процессами через сокет-файл.
   7. Inotify Wrapper: Напиши модуль, который следит за изменениями в директории через inotify.
   8. CPU Affinity: Привяжи поток к конкретному ядру процессора через pthread_setaffinity_np.
   9. Anonymous Memory: Выдели страницу памяти через mmap и используй её под std::span.
   10. Pipe Stream: Создай конвейер (pipe) и перенаправь вывод одного процесса на вход другому.
   11. Environment Variables: Напиши безопасную обертку над getenv возвращающую optional.
   12. Shared Memory: Создай сегмент разделяемой памяти между двумя независимыми запусками программы.
   13. Resource Limits: Используй getrlimit для получения информации о лимитах памяти процесса.
   14. Stat Module: Напиши обертку над stat, возвращающую структуру с метаданными файла через модули.
   15. Terminal Raw Mode: Переведи терминал в raw mode (чтение клавиш без Enter) и верни обратно через RAII.
   16. Cgroup Check: Напиши код, определяющий, запущен ли он в контейнере (Docker) через чтение /proc/self/cgroup.
   17. Fast I/O: Сравни скорость std::cin и чтения через fread в буфер std::vector<char>.
   18. POSIX Threads Interaction: Смешай std::jthread и нативные pthread-функции (например, установка приоритета).
   19. Timerfd: Используй timerfd_create для создания таймера, который можно ждать в epoll.
   20. Eventfd: Реализуй сигнализацию между потоками через eventfd.
   21. Self-Exe Path: Узнай путь к своему исполняемому файлу через /proc/self/exe.
   22. UUID Generator: Напиши обертку над /dev/urandom для генерации случайных ID.
   23. Dynamic Loading: Загрузи .so библиотеку через dlopen и вызови функцию по имени.
   24. Pselect/Epoll: Реализуй мультиплексирование ввода с клавиатуры и таймера.
   25. System Info: Напиши модуль, возвращающий количество RAM и загрузку CPU (через /proc/meminfo).

## Блок 7: Алгоритмы, Структуры данных и Ranges (151-175)
Фокус: высокая производительность и современный STL.

   1. Custom View: Реализуй my_views::trim, который убирает пробелы у строки (lazy).
   2. Stable Partition: Раздели вектор на четные и нечетные с сохранением порядка через std::ranges.
   3. Binary Search Tree (BST): Реализуй дерево на std::unique_ptr внутри модуля.
   4. Trie (Префиксное дерево): Реализуй поиск слов по префиксу.
   5. LRU Cache: Создай кэш последних использованных элементов на базе std::list и std::unordered_map.
   6. Ring Buffer: Реализуй циклический буфер фиксированного размера.
   7. Heap Sort: Реализуй сортировку кучей, используя std::make_heap и std::pop_heap.
   8. Set Intersection: Найди общие элементы двух отсортированных векторов через std::ranges::set_intersection.
   9. Adjacent Find: Найди первую пару одинаковых элементов в последовательности.
   10. MinMax Element: Найди минимальный и максимальный элементы за один проход.
   11. Permutations: Выведи все перестановки строки через std::ranges::next_permutation.
   12. Rotate Range: Сдвинь элементы массива циклически на N позиций.
   13. Partial Sum: Реализуй накопленную сумму (сканирование) через std::ranges.
   14. Disjoint Set Union (DSU): Реализуй систему непересекающихся множеств.
   15. Graph Adjacency List: Представь граф через std::vector<std::vector<int>> в модуле.
   16. BFS/DFS: Реализуй обход графа.
   17. Dijkstra Algorithm: Найди кратчайший путь в графе.
   18. String Hashing: Реализуй алгоритм полиномиального хеширования строки.
   19. Bitset Sieve: Решето Эратосфена на std::bitset.
   20. Bloom Filter: Реализуй вероятностную структуру данных для проверки наличия элемента.
   21. Sparse Matrix: Реализуй разреженную матрицу через словарь координат.
   22. Fenwick Tree: Реализуй дерево Фенвика для суммы на отрезке.
   23. Monotonic Stack: Найди ближайший больший элемент для каждого элемента массива за O(N).
   24. Kadane’s Algorithm: Найди подмассив с максимальной суммой.
   25. Topological Sort: Реализуй топологическую сортировку для ориентированного графа.

## Блок 8: Корутины и Продвинутый Async (176-200)
Фокус: std::coroutine и асинхронное состояние.

   1. Generator T: Напиши корутину-генератор последовательности Фибоначчи.
   2. Awaitable Echo: Напиши корутину, которая «засыпает», пока в std::cin не появится ввод.
   3. Coroutine Promise: Реализуй свой promise_type для кастомного объекта будущего (Future).
   4. Yield String: Корутина, которая посимвольно выдает содержимое файла.
   5. Async Task Wrapper: Оберни вызов std::async в корутину так, чтобы результат можно было ждать через co_await.
   6. State Machine Coroutine: Реализуй парсер протокола через корутины как конечный автомат.
   7. Recursive Coroutine: Реализуй обход дерева через корутины.
   8. Memory Order SeqCst: Реализуй алгоритм Петерсона для синхронизации двух потоков.
   9. Lock-Free Queue (SPSC): Реализуй очередь для одного производителя и одного потребителя.
   10. Atomic Smart Ref: Реализуй подсчет ссылок через std::atomic вручную.
   11. Wait-Free Counter: Реализуй счетчик, где каждый поток пишет в свой слот, а сумма собирается редко.
   12. Spinlock with Backoff: Реализуй спинлок, который делает паузы (_mm_pause) при неудаче.
   13. Priority Jthread: Создай пул потоков, где задачи имеют приоритет.
   14. Async File IO: Оберни aio_read (Linux) в интерфейс с корутинами.
   15. Condition Variable Any: Используй std::condition_variable_any вместе с std::stop_token.
   16. Thread-Safe Singleton (Double Check): Реализуй синглтон через std::atomic с memory_order_acquire/release.
   17. Coroutine Cancellation: Реализуй механизм отмены работающей корутины через stop_token.
   18. Batch Processing: Реализуй систему, которая накапливает 10 задач и только потом отдает их на выполнение в поток.
   19. Atomic View: Используй std::atomic_ref для атомарной работы с элементом обычного вектора.
   20. Custom Allocator: Напиши простейший аллокатор на базе пула памяти (pool allocator).
   21. Shared Memory Coroutine: Организуй передачу сигналов через shm_open и корутины.
   22. Semaphore-based Throttle: Ограничь частоту выполнения функции (Rate Limiting).
   23. Wait-Free Ring Buffer: Реализуй кольцевой буфер без мьютексов.
   24. Parallel Transform Reduce: Вычисли среднее значение огромного массива параллельно.
   25. The Final System Boss: Напиши асинхронный логгер, который пишет в файл в отдельном потоке, использует корутины для ожидания новых записей и модули для организации кода.


Третья сотня — это уровень Expert / Low-Level Engineering. Здесь мы уходим в экстремальное метапрограммирование, глубокие оптимизации производительности (Performance Engineering), безопасность и проектирование полноценных систем.
Поскольку у тебя Clang 20, мы будем выжимать максимум из того, что он умеет в плане отладки, интроспекции и работы с памятью.
## Блок 9: Hardcore Metaprogramming & Reflection (201-225)
Цель: заставить компилятор писать код за тебя.

   1. Type-to-String: Напиши шаблон, который возвращает имя типа в виде std::string_view (используй __PRETTY_FUNCTION__).
   2. Tuple Slice: Реализуй функцию, которая возвращает подмножество элементов кортежа с индекса по индекс.
   3. Compile-time JSON Parser: Реализуй парсинг простейшей JSON-строки в static_assert.
   4. Deducing This Proxy: Реализуй паттерн Proxy, который пробрасывает const/ref квалификаторы через this Auto&& self.
   5. Static Interface Checker: Напиши концепт, который проверяет, что класс реализует список методов с точными сигнатурами.
   6. Manual VTable: Реализуй динамический полиморфизм вручную через массив указателей на функции (без virtual).
   7. Constexpr Hash Map: Реализуй хеш-таблицу с открытой адресацией, работающую в compile-time.
   8. Expression Templates: Реализуй ленивое сложение векторов D = A + B + C без создания промежуточных объектов.
   9. Function Overload Set: Создай объект-обертку, который объединяет несколько функций в один набор перегрузок.
   10. Universal Any: Реализуй свой any тип через std::aligned_storage и ручное управление временем жизни.
   11. Injecting Base Classes: Напиши шаблон, который наследуется от всех типов в Parameter Pack.
   12. Compile-time Sort Comparison: Сравни скорость сборки двух реализаций constexpr sort: на рекурсии и на циклах.
   13. Member Accessor: Напиши макрос + шаблон для доступа к приватным полям класса (через явную специализацию шаблона).
   14. Custom Literal: Реализуй литерал _bin, чтобы писать auto x = "1101"_bin (результат — int).
   15. Type Erasure (Small Buffer): Реализуй std::function вручную с оптимизацией для малых объектов.
   16. Variadic Zip Apply: Примени функцию к элементам двух разных tuple параллельно.
   17. Constexpr Virtual: Создай иерархию классов с виртуальными функциями, работающую в constexpr (C++20+).
   18. Bitfield DSL: Напиши систему описания регистров процессора через шаблоны для типобезопасной работы.
   19. Predicate Composition: Реализуй операторы && и || для концептов или лямбд.
   20. Static Polymorphism (CRTP 2.0): Реализуй систему плагинов на базе CRTP и модулей.
   21. Type Erasure Interface: Реализуй аналог std::any, но ограниченный интерфейсом Drawable.
   22. Constexpr Regex: Реализуй простейший движок регулярных выражений в compile-time.
   23. SFINAE-friendly Dispatch: Напиши диспетчер функций, который игнорирует типы без нужного метода.
   24. Template Recursion Limit: Напиши код, который находит предел глубины рекурсии шаблонов твоего Clang.
   25. Named Arguments: Реализуй вызов функции с именованными аргументами через operator=.

## Блок 10: Performance Engineering & SIMD (226-250)
Цель: писать код, который «летает» на современном CPU.

   1. Cache Line Alignment: Напиши бенчмарк, показывающий влияние False Sharing на атомики.
   2. Prefetching: Используй __builtin_prefetch для ускорения обхода связанного списка.
   3. SIMD Add: Сложи два массива по 1000 элементов, используя интринсики _mm256_add_ps (AVX2).
   4. Loop Unrolling: Напиши шаблон, который разворачивает цикл на N итераций.
   5. Branch Prediction: Напиши код, который работает в 10 раз быстрее после сортировки входного массива (эффект бранч-предиктора).
   6. Custom Vector (No-Init): Реализуй вектор, который не инициализирует память нулями при выделении.
   7. AOS to SOA: Преобразуй массив структур (Array of Structures) в структуру массивов для лучшего использования кэша.
   8. Fast Math: Сравни точность и скорость вычислений с флагом -ffast-math и без него.
   9. Memory Pool (Arena): Реализуй арена-аллокатор, который освобождает всё одним махом.
   10. Object Pool: Реализуй пул объектов для быстрого переиспользования в высоконагруженных системах.
   11. Huge Pages: Выдели память через mmap с флагом MAP_HUGETLB.
   12. Zero-Copy Parser: Напиши парсер сетевых пакетов, который работает только на std::span и string_view.
   13. Bit Manipulation: Реализуй алгоритм подсчета установленных бит (popcount) через std::bitset и интринсики.
   14. Value Speculation: Реализуй код, который предполагает результат и делает откат при ошибке.
   15. Matrix Transpose Optimization: Реализуй блочное транспонирование матрицы для кэш-эффективности.
   16. Small Object Allocator: Напиши аллокатор, специализирующийся на объектах до 64 байт.
   17. Virtual Table De-virtualization: Напиши код, который помогает компилятору инлайнить виртуальные вызовы.
   18. Noexcept Performance: Проверь, как noexcept влияет на генерацию кода и оптимизации вектора.
   19. Intrinsics String Search: Реализуй быстрый поиск подстроки через SSE/AVX.
   20. Non-temporal Stores: Используй _mm_stream_si128 для записи данных в память в обход кэша.
   21. Stack-only Class: Напиши класс, который физически невозможно создать в куче.
   22. TCMalloc/Jemalloc Sim: Попробуй подменить стандартный new своим аллокатором в модуле.
   23. Data Locality Test: Сравни скорость обхода std::vector против std::list на 1 млн элементов.
   24. Micro-benchmarking: Напиши свою обертку над std::chrono, которая учитывает прогрев CPU.
   25. Binary Bloat Check: Исследуй, как использование шаблонов и модулей влияет на размер бинарника.

## Блок 11: Security & Robust Systems (251-275)
Цель: код, который нельзя сломать.

   1. Secure String: Реализуй строку, которая затирает память нулями в деструкторе (std::erase_free).
   2. Integer Overflow Check: Напиши обертку над int, которая кидает исключение при переполнении (используй встроенные функции Clang).
   3. Safe Casting: Напиши аналог checked_cast, который проверяет диапазоны в рантайме.
   4. Address Sanitizer (ASan): Напиши код с утечкой и научись ловить её через флаги компиляции в Linux.
   5. Thread Sanitizer (TSan): Создай Data Race и поймай его.
   6. Fuzz Testing Stubs: Подготовь функцию для тестирования через LLVMFuzzerTestOneInput.
   7. Stack Protector: Проверь, как работает -fstack-protector при выходе за границы массива на стеке.
   8. Strong Types: Реализуй обертку UserId и OrderId, чтобы их нельзя было перепутать (хотя оба int).
   9. Constexpr Parser Validation: Проверяй корректность конфиг-файла прямо во время компиляции.
   10. Immutable Data Structures: Реализуй дерево, которое при изменении создает новые узлы, не меняя старые.
   11. RAII Capabilities: Реализуй систему прав доступа, где наличие объекта-токена разрешает вызов функции.
   12. Sandboxed Callback: Реализуй механизм безопасного вызова пользовательской функции с перехватом исключений.
   13. Input Sanitization: Напиши модуль для очистки строк от потенциальных SQL/Command инъекций.
   14. Policy-based Design: Реализуй класс логгера, где стратегия блокировки (мьютекс или ничего) выбирается шаблоном.
   15. Dependency Injection (Static): Реализуй DI через модули и шаблоны без использования интерфейсов.
   16. Sealed Classes: Эмулируй final иерархию, где новые наследники запрещены вне модуля.
   17. Opaque Pointers (Pimpl): Реализуй паттерн Pimpl, используя std::unique_ptr и модули.
   18. Resource Leak Tracker: Напиши систему, которая считает количество живых объектов в реальном времени.
   19. Abseil-style Failure: Реализуй макросы CHECK и DCHECK, которые валят программу с дампом переменных.
   20. Compile-time Bounds Check: Используй std::span с проверкой границ в режиме отладки.
   21. Signal-Safe Queue: Напиши очередь, в которую можно безопасно писать внутри обработчика сигналов.
   22. Error Recovery: Реализуй паттерн "Retry" для функций, возвращающих std::expected.
   23. Atomic Smart Flag: Реализуй флаг, который можно взвести только один раз из любого потока.
   24. Memory Fence Debug: Напиши тест, доказывающий необходимость memory_order на слабой архитектуре (или через симулятор).
   25. API Versioning: Реализуй поддержку двух версий API в модулях через inline namespace.

## Блок 12: High-Level Architecture & Final Projects (276-300)
Цель: собрать всё воедино.

   1. Command Dispatcher: Система регистрации и вызова команд по строковому имени через std::map и лямбды.
   2. Pub/Sub System: Реализуй шину сообщений внутри программы.
   3. Actor Model (Basic): Каждый объект — поток, общение через очереди сообщений.
   4. Object-Relational Mapping (Minimal): Мапинг структуры C++ на SQL запрос через рефлексию (или её имитацию).
   5. Plug-in System: Загрузка функционала из .so файлов с регистрацией в модуле.
   6. Small Scripting Engine: Напиши интерпретатор выражений (например, 2 + 3 * x).
   7. Virtual File System: Слой абстракции над диском и памятью.
   8. Memory-Mapped Database: Простейшее хранилище ключ-значение на базе mmap.
   9. Async Web Server (Stubs): Реализуй цикл обработки событий (Event Loop) на epoll.
   10. Custom Coroutine Scheduler: Свой планировщик, который распределяет корутины по пулу потоков.
   11. Distributed Counter: Счетчик, синхронизируемый через сокеты.
   12. Reflection-based Serializer: Сериализация в XML/JSON одной строчкой кода.
   13. Undo/Redo Framework: Система отката действий на базе паттерна Команда.
   14. High-Frequency Timer: Таймер с точностью до наносекунд для Linux.
   15. Lock-free Hash Table: (Сложно!) Реализуй таблицу, в которой чтение всегда идет без блокировок.
   16. Entity-Component System (ECS): Базовая архитектура для игрового движка.
   17. Compile-time State Machine: Генерация эффективного кода автомата из описания переходов.
   18. Network Protocol State Machine: Обработка состояний TCP-соединения.
   19. Parallel Image Filter: Обработка массива пикселей через SIMD и потоки.
   20. Thread-safe Logger: Логгер с буферизацией и записью в отдельном системном потоке.
   21. Resource Manager: Автоматическая загрузка и выгрузка текстур/звуков по требованию.
   22. Type-safe SQL Query Builder: Чтобы нельзя было написать ошибочный запрос.
   23. Custom Debugger Trace: Интеграция с GDB через __builtin_debugtrap.
   24. Memory Statistics Module: Модуль, перехватывающий malloc/free для анализа фрагментации.
   25. The Final Masterpiece: Напиши свою мини-ОС (ядро библиотеки), которая включает в себя: модули, планировщик задач, систему обработки ошибок на expected и lock-free очередь сообщений.

Совет: Поскольку ты работаешь со std::print и modules, старайся в каждой задаче из 3-й сотни минимизировать использование макросов. C++26 — это про то, как делать метапрограммирование чистым.
Готов начать с первой задачи этого блока? Какую выберешь для разминки?
